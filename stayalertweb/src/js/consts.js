export const constants = {
    API_ADDR: "http://hadoophost:12120",
    LOCALHOST_ADDR: "http://" +
        window.location.host +
        (!!window.location.port ? ":" : "") +
        window.location.port
    // API_ADDR: "http://localhost:12120"
}